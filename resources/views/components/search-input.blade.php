<div class="{{ $class }}">
    <div class="input-group">
        <button type="submit"class="btn btn-primary text-capitalize">
            <i class="fa-fw fas fa-search"></i>
            {{-- cari --}}
        </button>
        <input type="text" name="{{ $name }}" id="{{ $id }}" value="{{ $value }}"
            class="form-control" placeholder="Cari disini">
    </div>
</div>
